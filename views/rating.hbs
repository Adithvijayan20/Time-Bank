<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="google-site-verification" content="QUhwJMeC4PDBYMpBnHttnhuEKagM0np4zi9cWmlbFNs" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Rate Completed Volunteer Work</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f7fc;
      margin: 0;
      padding: 0;
      color: #333;
    }

    h1 {
      text-align: center;
      color: #2a2f45;
      margin-top: 50px;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    ul {
      list-style-type: none;
      padding: 0;
    }

    li {
      background-color: #fafafa;
      padding: 20px;
      margin-bottom: 20px;
      border-radius: 8px;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    }

    h3 {
      font-size: 1.2em;
      color: #2a2f45;
    }

    p {
      color: #666;
    }

    form {
      margin-top: 20px;
      display: flex;
      flex-direction: column;
    }

    label {
      margin-bottom: 8px;
      font-weight: bold;
      color: #444;
    }

    input[type="number"] {
      padding: 8px;
      font-size: 1em;
      border: 1px solid #ddd;
      border-radius: 5px;
      width: 100px;
      margin-bottom: 12px;
      transition: border-color 0.3s;
    }

    input[type="number"]:focus {
      border-color: #007BFF;
      outline: none;
    }

    button {
      background-color: #007BFF;
      color: #fff;
      padding: 10px;
      font-size: 1.1em;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s;
    }

    button:hover {
      background-color: #0056b3;
    }

    .no-volunteers {
      text-align: center;
      font-size: 1.2em;
      color: #888;
    }
  </style>
</head>
<body>
  <h1>Rate Your Completed Volunteer Work</h1>

  <div class="container">
    {{#if completedMatches.length}}
      <ul>
        {{#each completedMatches}}
          <li>
            <h3>Volunteer: {{volunteerName}}</h3>
            <p>Service: 
              {{#if services.length}}
                {{#each services}}
                  {{this}}{{#unless @last}}, {{/unless}}
                {{/each}}
              {{else}}
                No services listed
              {{/if}}
            </p>
            <form action="/rate-volunteer/{{_id}}" method="POST">
              <label for="rating-{{_id}}">Your Rating (1-5):</label>
              <input id="rating-{{_id}}" type="number" name="rating" min="1" max="5" required>
              <button type="submit">Submit Rating</button>
            </form>
          </li>
        {{/each}}
      </ul>
    {{else}}
      <p class="no-volunteers">No completed volunteer work available for rating.</p>
    {{/if}}
  </div>
</body>
</html>
