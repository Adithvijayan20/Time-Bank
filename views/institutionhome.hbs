<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Institution Dashboard - TimeBank</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css">
    <style>
        :root {
            --primary-color: #4CAF50;
            --primary-dark: #45a049;
            --secondary-color: #2196F3;
            --warning-color: #FF9800;
            --danger-color: #F44336;
            --success-color: #8BC34A;
            --light-color: #f5f5f5;
            --dark-color: #333333;
            --shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }
        
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            margin: 0;
            padding: 0;
            background: #f0f5f0;
            color: var(--dark-color);
            position: relative;
            min-height: 100vh;
        }
        
        header {
            background-color: rgba(76, 175, 80, 0.9);
            color: #ffffff;
            padding: 1rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: var(--shadow);
        }
        
        header h1 {
            margin: 0;
            font-size: 1.8rem;
        }
        
        .button-group {
            display: flex;
            gap: 1rem;
        }
        
        .button-group a {
            background-color: #ffffff;
            color: var(--primary-color);
            border: 2px solid var(--primary-color);
            padding: 0.5rem 1rem;
            border-radius: 5px;
            text-decoration: none;
            font-weight: bold;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .button-group a:hover {
            background-color: var(--primary-color);
            color: #ffffff;
            transform: translateY(-2px);
        }
        
        main {
            padding: 2rem;
            max-width: 1200px;
            margin: 2rem auto;
        }
        
        .dashboard-welcome {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            padding: 1.5rem;
            margin-bottom: 2rem;
            text-align: center;
        }
        
        .dashboard-welcome h2 {
            color: var(--primary-color);
            margin-top: 0;
        }
        
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .stat-box {
            background: white;
            border-radius: 10px;
            padding: 1.5rem;
            box-shadow: var(--shadow);
            text-align: center;
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 180px;
        }
        
        .stat-box:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
        }
        
        .stat-box .icon {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }
        
        .stat-box.total-volunteers .icon {
            color: var(--secondary-color);
        }
        
        .stat-box.active-volunteers .icon {
            color: var(--success-color);
        }
        
        .stat-box.blocked-volunteers .icon {
            color: var(--danger-color);
        }
        
        .stat-box.daily-services .icon {
            color: var(--warning-color);
        }
        
        .stat-box h3 {
            margin: 0;
            font-size: 1.2rem;
            color: var(--dark-color);
        }
        
        .stat-box .count {
            font-size: 2.5rem;
            font-weight: bold;
            margin: 0.5rem 0;
            color: var(--dark-color);
        }
        
        .stat-box .description {
            color: #666;
            font-size: 0.9rem;
        }
        
        .performance-section {
            background: white;
            border-radius: 10px;
            box-shadow: var(--shadow);
            padding: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .performance-section h3 {
            color: var(--primary-color);
            margin-top: 0;
            margin-bottom: 1.5rem;
            border-bottom: 2px solid var(--primary-color);
            padding-bottom: 0.5rem;
        }
        
        .volunteer-services {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 1rem;
        }
        
        .volunteer-card {
            background: #ffffff;
            border-radius: 8px;
            box-shadow: var(--shadow);
            padding: 1rem;
            transition: var(--transition);
            border-left: 4px solid var(--secondary-color);
        }
        
        .volunteer-card:hover {
            transform: translateX(5px);
        }
        
        .volunteer-card .volunteer-info {
            display: flex;
            justify-content: space-between;
            margin-bottom: 0.5rem;
        }
        
        .volunteer-card .volunteer-name {
            font-weight: bold;
            color: var(--dark-color);
        }
        
        .volunteer-card .services-count {
            background-color: var(--secondary-color);
            color: white;
            padding: 0.2rem 0.5rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
        }
        
        .progress-bar {
            height: 8px;
            background-color: #f0f0f0;
            border-radius: 4px;
            overflow: hidden;
            margin-top: 0.5rem;
        }
        
        .progress-bar .progress {
            height: 100%;
            background-color: var(--success-color);
            width: 0;
        }
        
        footer {
            text-align: center;
            padding: 1rem;
            background-color: rgba(241, 241, 241, 0.9);
            position: relative;
            bottom: 0;
            width: 100%;
        }
        
        .filters {
            display: flex;
            justify-content: space-between;
            margin-bottom: 1rem;
        }
        
        .filter-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        select, input {
            padding: 0.5rem;
            border-radius: 5px;
            border: 1px solid #ddd;
        }
        
        .pagination {
            display: flex;
            justify-content: center;
            margin-top: 1rem;
            gap: 0.5rem;
        }
        
        .pagination button {
            background-color: white;
            border: 1px solid #ddd;
            padding: 0.5rem 0.8rem;
            border-radius: 5px;
            cursor: pointer;
            transition: var(--transition);
        }
        
        .pagination button.active {
            background-color: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
        }
        
        .pagination button:hover:not(.active) {
            background-color: #f1f1f1;
        }
        
        .no-data-message {
            text-align: center;
            padding: 2rem;
            color: #666;
        }
    </style>
</head>
<body>
    <!-- Hidden input to store dashboard data from server -->
    <input type="hidden" id="dashboard-data" value='<%= dashboardStats %>'>
    <input type="hidden" id="app-name" value="<%= appName %>">
    
    <header>
        <h1 id="app-name-header">{{appName}} - Institution Dashboard</h1>
        <div class="button-group">
            <a href="/institution/profile"><i class="fas fa-user-circle"></i> Profile Update</a>
            <a href="/institution/addvolunteers"><i class="fas fa-hands-helping"></i> Add Volunteer</a>
            <a href="/institution/activeServices"><i class="fas fa-hands-helping"></i> Active Services</a>
            <a href="/institution/notifications">
                <i class="fas fa-bell"></i> 
                Notifications 
                {{#if unreadNotificationCount}}
                    <span class="badge">{{unreadNotificationCount}}</span>
                {{/if}}
            </a>
            <a href="/institution/viewvolunteers"><i class="fas fa-list"></i> View Volunteers</a>
            <a href="/"><i class="fas fa-sign-out-alt"></i> Logout</a>
        </div>
    </header>
    
    <main>
        <div class="dashboard-welcome animate__animated animate__fadeIn">
            <h2>Welcome, <span id="institution-name">{{institutionName}}</span>!</h2>
            <p>Here's an overview of your volunteer management and service delivery.</p>
        </div>
        
        <div class="stats-container animate__animated animate__fadeInUp">
            <div class="stat-box total-volunteers">
                <div class="icon"><i class="fas fa-users"></i></div>
                <h3>Total Volunteers</h3>
                <div class="count" id="total-volunteers">
                    {{#if dashboardStats.totalVolunteers}}
                        {{dashboardStats.totalVolunteers}}
                    {{else}}
                        0
                    {{/if}}
                </div>
                <div class="description">Registered volunteers in your system</div>
            </div>
            
            <div class="stat-box active-volunteers">
                <div class="icon"><i class="fas fa-user-check"></i></div>
                <h3>Active Volunteers</h3>
                <div class="count" id="active-volunteers">
                    {{#if dashboardStats.activeVolunteers}}
                        {{dashboardStats.activeVolunteers}}
                    {{else}}
                        0
                    {{/if}}
                </div>
                <div class="description">Currently active and available</div>
            </div>
            
            <div class="stat-box blocked-volunteers">
                <div class="icon"><i class="fas fa-user-lock"></i></div>
                <h3>Blocked Volunteers</h3>
                <div class="count" id="blocked-volunteers">0</div>
                <div class="description">Temporarily suspended accounts</div>
            </div>
            
            <div class="stat-box daily-services">
                <div class="icon"><i class="fas fa-chart-line"></i></div>
                <h3>Today's Services</h3>
                <div class="count" id="daily-services">
                    {{#if dashboardStats.dailyServices}}
                        {{dashboardStats.dailyServices}}
                    {{else}}
                        0
                    {{/if}}
                </div>
                <div class="description">Services completed today</div>
            </div>
        </div>
        
        <div class="performance-section animate__animated animate__fadeIn">
            <h3><i class="fas fa-award"></i> Volunteer Service Performance</h3>
            
            <div class="filters">
                <div class="filter-item">
                    <label for="sort-by">Sort By:</label>
                    <select id="sort-by">
                        <option value="highest">Highest Services</option>
                        <option value="lowest">Lowest Services</option>
                        <option value="name">Name</option>
                    </select>
                </div>
                
                <div class="filter-item">
                    <label for="search">Search:</label>
                    <input type="text" id="search" placeholder="Volunteer name...">
                </div>
            </div>
            
            <div id="volunteer-services" class="volunteer-services">
                <!-- Volunteer service cards will be populated here by JavaScript -->
            </div>
            
            <div class="pagination" id="pagination">
                <!-- Pagination will be populated here by JavaScript -->
            </div>
        </div>
    </main>
    
    <footer>
        <p>&copy; <span class="app-name-footer">{{appName}}</span>. All rights reserved.</p>
    </footer>
</body>
</html>